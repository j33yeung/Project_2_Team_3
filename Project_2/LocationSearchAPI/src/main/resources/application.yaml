server:
  port: 8081
#  servlet:
#    context-path: /reimbursement

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/LocationSearchAPI_db
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true